spring.application.name=sentimentapi
server.servlet.context-path=/project/api/v2

# ============================================================
# Puerto del servidor (Render asigna PORT dinamicamente)
# ============================================================
server.port=${SERVER_PORT:8080}

# ============================================================
# Configuracion API Python (Render)
# ============================================================
config.url=${CONFIG_URL:https://sentiment-api-render.onrender.com}

# ============================================================
# Conexion con PostgreSQL (Render)
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d5vdmql6ubrc73cd72ig-a.oregon-postgres.render.com:5432/sentiment_db_ay8e}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sentiment_db_ay8e_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:REXGv3PExiH2DuU4OV1MdBZ0sufnQSOH}

# ============================================================
# Configuracion JPA/Hibernate
# ============================================================
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ============================================================
# Configuracion JWT
# ============================================================
jwt.secret=${JWT_SECRET:sentimentApiSecretKey2026HackathonOneNoCountry}
jwt.expiration=${JWT_EXPIRATION:86400000}
